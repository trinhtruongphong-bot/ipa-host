services:
  - type: web
    name: ipa-server-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py & python bot.py
    plan: free
    envVars:
      - key: GITHUB_TOKEN
        sync: false
      - key: GITHUB_REPO
        sync: false
      - key: GITHUB_BRANCH
        sync: false
      - key: TELEGRAM_TOKEN
        sync: false
      - key: WEB_CONCURRENCY
        value: 1
